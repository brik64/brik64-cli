// brik64.pcd_file.v1
// monomer: MC_42.SUBSTR
// tier: core
// claim_boundary: source_contract_candidate
PC MC_42_SUBSTR {
    fn MC_42_SUBSTR(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
