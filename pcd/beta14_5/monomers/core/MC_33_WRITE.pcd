// brik64.pcd_file.v1
// monomer: MC_33.WRITE
// tier: core
// claim_boundary: source_contract_candidate
PC MC_33_WRITE {
    fn MC_33_WRITE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
