// brik64.pcd_file.v1
// monomer: MC_30.CONTINUE
// tier: core
// claim_boundary: source_contract_candidate
PC MC_30_CONTINUE {
    fn MC_30_CONTINUE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
