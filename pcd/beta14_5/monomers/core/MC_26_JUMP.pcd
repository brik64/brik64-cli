// brik64.pcd_file.v1
// monomer: MC_26.JUMP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_26_JUMP {
    fn MC_26_JUMP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
