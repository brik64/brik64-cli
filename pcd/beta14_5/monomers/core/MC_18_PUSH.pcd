// brik64.pcd_file.v1
// monomer: MC_18.PUSH
// tier: core
// claim_boundary: source_contract_candidate
PC MC_18_PUSH {
    fn MC_18_PUSH(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
