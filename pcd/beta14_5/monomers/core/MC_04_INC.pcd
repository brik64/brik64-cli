// brik64.pcd_file.v1
// monomer: MC_04.INC
// tier: core
// claim_boundary: source_contract_candidate
PC MC_04_INC {
    fn MC_04_INC(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
