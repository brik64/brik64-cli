// brik64.pcd_file.v1
// monomer: MC_45.LOWER
// tier: core
// claim_boundary: source_contract_candidate
PC MC_45_LOWER {
    fn MC_45_LOWER(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
