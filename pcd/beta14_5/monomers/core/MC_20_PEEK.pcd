// brik64.pcd_file.v1
// monomer: MC_20.PEEK
// tier: core
// claim_boundary: source_contract_candidate
PC MC_20_PEEK {
    fn MC_20_PEEK(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
