// brik64.pcd_file.v1
// monomer: MC_63.ENV
// tier: core
// claim_boundary: source_contract_candidate
PC MC_63_ENV {
    fn MC_63_ENV(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
