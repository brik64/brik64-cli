// brik64.pcd_file.v1
// monomer: MC_10.XOR8
// tier: core
// claim_boundary: source_contract_candidate
PC MC_10_XOR8 {
    fn MC_10_XOR8(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
