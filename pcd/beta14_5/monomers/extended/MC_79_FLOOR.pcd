// brik64.pcd_file.v1
// monomer: MC_79.FLOOR
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_79_FLOOR {
    fn MC_79_FLOOR(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
