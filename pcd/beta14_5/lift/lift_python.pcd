// brik64.pcd_file.v1
// lift_language: python
// claim_boundary: source_contract_candidate
PC lift_python {
    fn lift_python(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
