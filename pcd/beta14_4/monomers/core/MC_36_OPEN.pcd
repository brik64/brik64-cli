// brik64.pcd_file.v1
// monomer: MC_36.OPEN
// tier: core
// claim_boundary: source_contract_candidate
PC MC_36_OPEN {
    fn MC_36_OPEN(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
