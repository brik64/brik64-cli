// brik64.pcd_file.v1
// monomer: MC_53.RNG
// tier: core
// claim_boundary: source_contract_candidate
PC MC_53_RNG {
    fn MC_53_RNG(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
