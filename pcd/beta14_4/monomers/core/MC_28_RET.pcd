// brik64.pcd_file.v1
// monomer: MC_28.RET
// tier: core
// claim_boundary: source_contract_candidate
PC MC_28_RET {
    fn MC_28_RET(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
