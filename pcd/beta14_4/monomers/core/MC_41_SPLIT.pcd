// brik64.pcd_file.v1
// monomer: MC_41.SPLIT
// tier: core
// claim_boundary: source_contract_candidate
PC MC_41_SPLIT {
    fn MC_41_SPLIT(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
