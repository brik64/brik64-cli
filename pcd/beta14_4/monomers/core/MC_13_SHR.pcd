// brik64.pcd_file.v1
// monomer: MC_13.SHR
// tier: core
// claim_boundary: source_contract_candidate
PC MC_13_SHR {
    fn MC_13_SHR(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
