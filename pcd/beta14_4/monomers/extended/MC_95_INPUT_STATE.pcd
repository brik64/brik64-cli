// brik64.pcd_file.v1
// monomer: MC_95.INPUT_STATE
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_95_INPUT_STATE {
    fn MC_95_INPUT_STATE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
