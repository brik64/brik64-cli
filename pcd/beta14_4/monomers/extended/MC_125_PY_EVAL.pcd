// brik64.pcd_file.v1
// monomer: MC_125.PY_EVAL
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_125_PY_EVAL {
    fn MC_125_PY_EVAL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
