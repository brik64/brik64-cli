// brik64.pcd_file.v1
// beta14_3_source_contract: cli_entrypoint
// claim_boundary: source_contract_candidate; requires L6+N5 materialization evidence before publication.
PC cli_entrypoint {
    fn cli_entrypoint(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
