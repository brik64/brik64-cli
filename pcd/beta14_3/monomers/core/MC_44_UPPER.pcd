// brik64.pcd_file.v1
// monomer: MC_44.UPPER
// tier: core
// claim_boundary: source_contract_candidate
PC MC_44_UPPER {
    fn MC_44_UPPER(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
