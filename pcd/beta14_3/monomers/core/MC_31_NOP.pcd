// brik64.pcd_file.v1
// monomer: MC_31.NOP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_31_NOP {
    fn MC_31_NOP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
