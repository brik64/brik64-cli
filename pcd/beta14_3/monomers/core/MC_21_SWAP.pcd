// brik64.pcd_file.v1
// monomer: MC_21.SWAP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_21_SWAP {
    fn MC_21_SWAP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
