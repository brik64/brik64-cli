// brik64.pcd_file.v1
// monomer: MC_14.ROL
// tier: core
// claim_boundary: source_contract_candidate
PC MC_14_ROL {
    fn MC_14_ROL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
