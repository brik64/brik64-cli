// brik64.pcd_file.v1
// monomer: MC_122.FFI_ALLOC
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_122_FFI_ALLOC {
    fn MC_122_FFI_ALLOC(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
