// brik64.pcd_file.v1
// monomer: MC_106.DIR_DELETE
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_106_DIR_DELETE {
    fn MC_106_DIR_DELETE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
