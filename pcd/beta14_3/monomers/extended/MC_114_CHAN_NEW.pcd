// brik64.pcd_file.v1
// monomer: MC_114.CHAN_NEW
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_114_CHAN_NEW {
    fn MC_114_CHAN_NEW(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
