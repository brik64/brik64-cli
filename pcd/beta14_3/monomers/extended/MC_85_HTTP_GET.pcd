// brik64.pcd_file.v1
// monomer: MC_85.HTTP_GET
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_85_HTTP_GET {
    fn MC_85_HTTP_GET(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
