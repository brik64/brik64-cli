// brik64.pcd_file.v1
// claim_boundary: assisted_generation_non_claim
PC rust_f64_polymer_codegen {
    domain has_f64_clamp: i64 [0, 1];
    domain has_polymer_context: i64 [0, 1];
    domain cargo_test_passed: i64 [0, 1];
    fn rust_f64_polymer_codegen(has_f64_clamp, has_polymer_context, cargo_test_passed) -> i64 {
        if (has_f64_clamp == 0) { return 0; }
        if (has_polymer_context == 0) { return 0; }
        if (cargo_test_passed == 0) { return 0; }
        return 1;
    }
}
