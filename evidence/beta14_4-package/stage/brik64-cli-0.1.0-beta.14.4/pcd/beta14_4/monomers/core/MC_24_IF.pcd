// brik64.pcd_file.v1
// monomer: MC_24.IF
// tier: core
// claim_boundary: source_contract_candidate
PC MC_24_IF {
    fn MC_24_IF(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
