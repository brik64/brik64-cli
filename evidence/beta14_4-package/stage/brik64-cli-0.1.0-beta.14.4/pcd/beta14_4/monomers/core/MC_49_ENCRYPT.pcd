// brik64.pcd_file.v1
// monomer: MC_49.ENCRYPT
// tier: core
// claim_boundary: source_contract_candidate
PC MC_49_ENCRYPT {
    fn MC_49_ENCRYPT(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
