// brik64.pcd_file.v1
// monomer: MC_38.EOF
// tier: core
// claim_boundary: source_contract_candidate
PC MC_38_EOF {
    fn MC_38_EOF(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
