// brik64.pcd_file.v1
// monomer: MC_46.TRIM
// tier: core
// claim_boundary: source_contract_candidate
PC MC_46_TRIM {
    fn MC_46_TRIM(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
