// brik64.pcd_file.v1
// monomer: MC_80.TCP_CONN
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_80_TCP_CONN {
    fn MC_80_TCP_CONN(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
