// brik64.pcd_file.v1
// monomer: MC_82.TCP_RECV
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_82_TCP_RECV {
    fn MC_82_TCP_RECV(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
