// brik64.pcd_file.v1
// monomer: MC_94.INPUT_POLL
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_94_INPUT_POLL {
    fn MC_94_INPUT_POLL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
