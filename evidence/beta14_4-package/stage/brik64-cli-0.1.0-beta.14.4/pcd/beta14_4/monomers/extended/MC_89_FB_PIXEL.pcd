// brik64.pcd_file.v1
// monomer: MC_89.FB_PIXEL
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_89_FB_PIXEL {
    fn MC_89_FB_PIXEL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
