// brik64.pcd_file.v1
// monomer: MC_127.JSON_EMIT
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_127_JSON_EMIT {
    fn MC_127_JSON_EMIT(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
