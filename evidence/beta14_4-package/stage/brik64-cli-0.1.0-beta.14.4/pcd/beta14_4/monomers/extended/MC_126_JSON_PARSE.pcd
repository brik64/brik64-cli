// brik64.pcd_file.v1
// monomer: MC_126.JSON_PARSE
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_126_JSON_PARSE {
    fn MC_126_JSON_PARSE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
