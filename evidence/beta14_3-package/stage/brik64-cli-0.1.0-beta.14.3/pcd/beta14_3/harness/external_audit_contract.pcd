// brik64.pcd_file.v1
// harness: external_audit_contract
// claim_boundary: source_contract_candidate
PC external_audit_contract {
    fn external_audit_contract(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
