// brik64.pcd_file.v1
// monomer: MC_93.FB_FLUSH
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_93_FB_FLUSH {
    fn MC_93_FB_FLUSH(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
