// brik64.pcd_file.v1
// monomer: MC_104.DIR_LIST
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_104_DIR_LIST {
    fn MC_104_DIR_LIST(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
