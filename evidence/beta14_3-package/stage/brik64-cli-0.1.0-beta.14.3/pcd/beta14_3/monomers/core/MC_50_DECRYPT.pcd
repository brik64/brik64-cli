// brik64.pcd_file.v1
// monomer: MC_50.DECRYPT
// tier: core
// claim_boundary: source_contract_candidate
PC MC_50_DECRYPT {
    fn MC_50_DECRYPT(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
