// brik64.pcd_file.v1
// monomer: MC_40.CONCAT
// tier: core
// claim_boundary: source_contract_candidate
PC MC_40_CONCAT {
    fn MC_40_CONCAT(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
