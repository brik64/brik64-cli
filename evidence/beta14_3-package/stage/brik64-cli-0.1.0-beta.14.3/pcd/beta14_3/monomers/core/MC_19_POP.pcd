// brik64.pcd_file.v1
// monomer: MC_19.POP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_19_POP {
    fn MC_19_POP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
