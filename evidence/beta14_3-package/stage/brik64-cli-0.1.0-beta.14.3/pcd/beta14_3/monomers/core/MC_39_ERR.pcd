// brik64.pcd_file.v1
// monomer: MC_39.ERR
// tier: core
// claim_boundary: source_contract_candidate
PC MC_39_ERR {
    fn MC_39_ERR(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
