// brik64.pcd_file.v1
// monomer: MC_08.AND8
// tier: core
// claim_boundary: source_contract_candidate
PC MC_08_AND8 {
    fn MC_08_AND8(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
