// brik64.pcd_file.v1
// monomer: MC_61.PID
// tier: core
// claim_boundary: source_contract_candidate
PC MC_61_PID {
    fn MC_61_PID(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
