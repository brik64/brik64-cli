// brik64.pcd_file.v1
// monomer: MC_51.SIGN
// tier: core
// claim_boundary: source_contract_candidate
PC MC_51_SIGN {
    fn MC_51_SIGN(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
