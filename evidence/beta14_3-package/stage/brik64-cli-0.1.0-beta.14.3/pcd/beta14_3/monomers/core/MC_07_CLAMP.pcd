// brik64.pcd_file.v1
// monomer: MC_07.CLAMP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_07_CLAMP {
    fn MC_07_CLAMP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
