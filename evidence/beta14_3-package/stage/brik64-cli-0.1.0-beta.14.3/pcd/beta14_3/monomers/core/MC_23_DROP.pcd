// brik64.pcd_file.v1
// monomer: MC_23.DROP
// tier: core
// claim_boundary: source_contract_candidate
PC MC_23_DROP {
    fn MC_23_DROP(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
