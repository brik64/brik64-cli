// brik64.pcd_file.v1
// monomer: MC_47.MATCH
// tier: core
// claim_boundary: source_contract_candidate
PC MC_47_MATCH {
    fn MC_47_MATCH(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
