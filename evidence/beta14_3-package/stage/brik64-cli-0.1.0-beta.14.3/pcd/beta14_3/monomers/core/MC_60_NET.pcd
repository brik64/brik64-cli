// brik64.pcd_file.v1
// monomer: MC_60.NET
// tier: core
// claim_boundary: source_contract_candidate
PC MC_60_NET {
    fn MC_60_NET(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
