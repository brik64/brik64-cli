// brik64.pcd_file.v1
// monomer: MC_12.SHL
// tier: core
// claim_boundary: source_contract_candidate
PC MC_12_SHL {
    fn MC_12_SHL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
