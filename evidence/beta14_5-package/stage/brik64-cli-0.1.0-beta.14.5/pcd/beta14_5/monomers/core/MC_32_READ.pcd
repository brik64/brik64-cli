// brik64.pcd_file.v1
// monomer: MC_32.READ
// tier: core
// claim_boundary: source_contract_candidate
PC MC_32_READ {
    fn MC_32_READ(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
