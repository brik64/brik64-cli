// brik64.pcd_file.v1
// monomer: MC_29.BREAK
// tier: core
// claim_boundary: source_contract_candidate
PC MC_29_BREAK {
    fn MC_29_BREAK(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
