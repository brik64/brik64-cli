// brik64.pcd_file.v1
// monomer: MC_06.ABS
// tier: core
// claim_boundary: source_contract_candidate
PC MC_06_ABS {
    fn MC_06_ABS(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
