// brik64.pcd_file.v1
// monomer: MC_59.DISK
// tier: core
// claim_boundary: source_contract_candidate
PC MC_59_DISK {
    fn MC_59_DISK(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
