// brik64.pcd_file.v1
// monomer: MC_34.SEEK
// tier: core
// claim_boundary: source_contract_candidate
PC MC_34_SEEK {
    fn MC_34_SEEK(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
