// brik64.pcd_file.v1
// monomer: MC_05.DEC
// tier: core
// claim_boundary: source_contract_candidate
PC MC_05_DEC {
    fn MC_05_DEC(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
