// brik64.pcd_file.v1
// monomer: MC_54.DERIVE
// tier: core
// claim_boundary: source_contract_candidate
PC MC_54_DERIVE {
    fn MC_54_DERIVE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
