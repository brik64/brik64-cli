// brik64.pcd_file.v1
// monomer: MC_43.LEN
// tier: core
// claim_boundary: source_contract_candidate
PC MC_43_LEN {
    fn MC_43_LEN(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
