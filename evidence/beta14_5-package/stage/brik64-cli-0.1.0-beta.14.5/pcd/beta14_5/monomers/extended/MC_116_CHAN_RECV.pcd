// brik64.pcd_file.v1
// monomer: MC_116.CHAN_RECV
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_116_CHAN_RECV {
    fn MC_116_CHAN_RECV(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
