// brik64.pcd_file.v1
// monomer: MC_97.AU_WRITE
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_97_AU_WRITE {
    fn MC_97_AU_WRITE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
