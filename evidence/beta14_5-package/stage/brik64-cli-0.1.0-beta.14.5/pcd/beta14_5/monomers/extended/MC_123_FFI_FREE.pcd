// brik64.pcd_file.v1
// monomer: MC_123.FFI_FREE
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_123_FFI_FREE {
    fn MC_123_FFI_FREE(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
