// brik64.pcd_file.v1
// monomer: MC_121.FFI_CALL
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_121_FFI_CALL {
    fn MC_121_FFI_CALL(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
