// brik64.pcd_file.v1
// monomer: MC_81.TCP_SEND
// tier: extended
// certification-profile: extended_contract
// claim_boundary: source_contract_candidate
PC MC_81_TCP_SEND {
    fn MC_81_TCP_SEND(command_id) {
        if (command_id == 1) { return 1; }
        return 0;
    }
}
